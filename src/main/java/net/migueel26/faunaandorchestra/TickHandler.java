package net.migueel26.faunaandorchestra;

import net.neoforged.bus.api.SubscribeEvent;
import net.neoforged.fml.common.EventBusSubscriber;
import net.neoforged.neoforge.event.tick.PlayerTickEvent;
import net.neoforged.neoforge.event.tick.ServerTickEvent;

/*@EventBusSubscriber(modid = FaunaAndOrchestra.MOD_ID, bus = EventBusSubscriber.Bus.GAME)*/
public class TickHandler {
    /*
    private static int count = 0;
    @SubscribeEvent
    public static void playerTickEvent(ServerTickEvent.Pre event) //CHOOSE THE EVENT THAT BEST SUITS YOUR NEEDS
    {
        if(count < 30)
        {
            count++;
        }
        else
        {
            count = 0;
            System.out.println("Hi!");
        }
    }
    */
}
